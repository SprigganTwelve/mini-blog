<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
    <div class="container">
        <a class="navbar-brand" href="{{ path('pages_post') }}">
            MiniBlog
        </a>

        <button class="navbar-toggler" type="button"
                data-bs-toggle="collapse"
                data-bs-target="#mainNavbar"
                aria-controls="mainNavbar"
                aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mainNavbar">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('pages_post') }}">
                        Home
                    </a>
                </li>

                {% if role is defined and role is not empty %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{path("view_user_list")}}">
                            Users
                        </a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{path("posts_management")}}">
                            Posts
                        </a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{path("comment_management")}}">
                            Comments
                        </a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('page_categories') }}">
                            Categories
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">
                            Logout
                        </a>
                    </li>
                {% else  %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">
                            Login
                        </a>
                    </li>
                {% endif %}

            </ul>

            <form class="d-flex" role="search">
                <input
                    class="form-control me-2"
                    type="search"
                    placeholder="Search..."
                    aria-label="Search"
                >
                <button class="btn btn-outline-light" type="submit">
                    Search
                </button>
            </form>
        </div>
    </div>
</nav>
